# 가설 백로그

> **역할 분담** — 기록소(`experiment-registry.md`)는 *실행된 것*, `data-facts.md`는 *확정된 사실*,
> 이 파일은 **아직 안 한 것과 기각된 것**을 담는다. 여기 항목이 실행되면 선보고를 등록하고
> report_id를 적어 이 파일에서 닫는다. 최종 갱신 2026-08-04 (EXP-014 반영, 신기록 3.6332).

## 오차 예산 (EXP-014 기준, LB **3.6332**)

**후보의 기대 이득은 추측이 아니라 이 표에서 계산한다.**

```
LB² = sim 구조 한계  +  도메인 갭  +  레벨 오분류
      4.343 (32.9%)     5.996 (45.4%)  2.861 (21.7%)   = 13.200
```


| 성분                   | 값     | 이것만 0이면        | 무엇이 줄이나        |
| -------------------- | ----- | -------------- | -------------- |
| sim 구조 한계 `s_sim²`   | 4.343 | 2.976 (−0.657) | ⚠️ **죽은 레버 — R6.** conv 계열에서 이 성분을 줄이면 갭이 더 크게 늘어 총합이 악화된다 |
| 도메인 갭 `gap²`         | 5.996 | 2.684 (**−0.949**) | 도메인 적응 — **유일하게 남은 큰 활성 레버** |
| 레벨 오분류 `(1−p)·63.77` | 2.861 | 3.215 (−0.418) | 레벨 분류기 (H1 완료, 남은 몫은 H2) |


`LB = √(2.084² + gap² + (1−p)·63.77)`, 현재 `gap = 2.4488`, **유효 `p = 0.9551`**.
계수 63.77 = `100·E[(1−s)²]` — **인접 오분류에만 유효**하다(2칸 이상이면 과소평가, EXP-006 참조).

> **홀드아웃 p는 2.5pp 할인해서 쓸 것.** EXP-013이 real 사이트홀드아웃에서 잰 98.06%가
> 리더보드에서는 유효 95.51%로 나왔다(EXP-014). real→real 선판정은 **작동하되 낙관 편향**이
> 있다. 이 할인은 경로 분리가 안 된 경험칙이다 — 일부는 성분 비독립이 p로 흡수된 것일 수 있다.

---

## 활성 후보


| #   | 가설                             | 겨냥     | 기대                        | 비용     | 판정                          |
| --- | ------------------------------ | ------ | ------------------------- | ------ | --------------------------- |
| H2  | test 사이트 복원 → 레벨 다수결           | 레벨     | p≈1.0 → **−0.418** (반토막)  | 중간     | 클러스터 순도 (라벨 없음, 간접)         |
| H3  | AdaBN 표본 확대 (real+test 86,652) | 갭      | 소폭 + 가설 검증                | 학습 0   | 리더보드                        |
| H4  | 블러·이방성 증강 재학습                  | 갭      | 미지                        | 학습 1회  | 리더보드                        |
| H5  | 자기학습 2단계 (우리 의사라벨)             | 갭      | 미지                        | 학습 1회  | 리더보드                        |
| H6  | CycleGAN sim→real 변환           | 갭      | 미지                        | 높음     | 리더보드 + §7 통계 모니터            |
| H7  | DANN 적대적 특징 정렬                 | 갭      | 미지                        | 중간     | 리더보드                        |
| H8  | 2-헤드(마스크+깊이) / `s` 이산화         | sim 한계 | **재평가 필요** (R6 참조)        | 학습 1회  | sim 홀드아웃 ❌ + 리더보드           |


### H2 — test 사이트 복원 · **기대 이득이 반토막 났다**

EXP-004에서 사이트 다수결이 **410/410 사이트 100퍼센트**였다. test는 익명화돼 있지만
25,988 ÷ 31 ≈ 838 사이트가 섞여 있을 뿐이고, 같은 원본 영상에서 잘린 crop은 조명·노이즈를
공유한다. 5~10장만 묶여도 이득이 있다.

**단, H1이 레벨 성분을 이미 6.50 → 2.86으로 깎았다.** p=1.0을 달성해도 남은 몫은 −0.418로
이전 추정 −0.889의 절반 이하다. 갭 축(−0.949)보다 작으므로 **우선순위는 갭 축 다음이다.**
잘못 묶으면 악화되는 위험도 그대로다.

### H3 — AdaBN 표본 · **갭 축 최우선 (가장 쌈)**

EXP-010에서 test(25,988) → real(60,664)만으로 −0.522가 나왔다. **미검증 가설**: `momentum=None`
누적평균은 배치 크기와 무관하게 배치마다 같은 가중을 주므로, 마지막 부분배치의 왜곡이
배치 수가 적을수록 크다(test 51배치 대 real 119배치). batch 크기를 나누어떨어지게 하거나
real+test를 합쳐 확인한다.

### H4 — 블러·이방성 증강

측정된 차이를 직접 겨냥한다: 기울기 비 x 1.70 / y 1.99, 자기상관 0.955 대 0.982(§7).
**방향 주의** — real을 날카롭게가 아니라 **sim을 뭉갠다.** H6이 학습으로 찾을 변환을 손으로
지정하는 셈이라 훨씬 싸고 안정적이다. H6보다 먼저.

### H6 — CycleGAN

상위권 인터뷰 근거(unpaired 변환, "transferred simulation"). 진척도는 loss가 아니라 **§7의
분포·기울기·자기상관이 real에 수렴하는지**로 본다. 위험: 기하가 바뀌면 sim depth GT와 픽셀
대응이 깨진다. H4가 정체된 뒤에만.

### H8 — 정식화 · **기대 이득 재평가 필요**

픽셀의 51퍼센트가 정확히 `s=0`인 zero-inflated 분포에 L1 회귀를 걸고 있다. 마스크+깊이 2-헤드,
또는 `s` bin 분류(DORN류).

**원래 기대치 −0.57은 이제 못 쓴다.** 그 값은 예산표의 `sim 구조 한계`를 0으로 만든다는 계산인데,
R6에서 **sim 구조를 좋게 만들수록 real이 나빠지는 단조 반전**이 3점으로 확인됐다. 이 후보가
겨냥하는 성분 자체가 conv 계열에서 음의 가치를 갖는다. 실행한다면 sim 홀드아웃 수치는 **판정에
일절 쓰지 말고**, 리더보드만으로 판정하되 기대 이득은 "미지"로 취급한다.

---

## 기각·종료


| #   | 가설                               | 근거                            | 결론                                                |
| --- | -------------------------------- | ----------------------------- | ------------------------------------------------- |
| R1  | **처음부터 학습한** conv 백본(UNetSmall w32)이 전이를 개선 | EXP-008, EXP-010A | **기각.** sim 1.8875(개선)인데 real 갭 6.275 = MLP의 2.6배. **범위 주의**로 남겼던 "사전학습 인코더는 별건" 은 R6에서 검증돼 함께 기각됐다 |
| R2  | 히스토그램 매칭                         | EXP-009 −0.23, EXP-010B −0.03 | **종료.** 주변분포를 소수점까지 맞춰도 무효. 점별 LUT는 공간 상관을 못 바꾼다  |
| R3  | `average_depth`를 학습 신호로          | 그룹내 1.53이 유일한 새 정보            | **기각.** 완벽히 맞혀도 상한 −0.07                          |
| R4  | avg 조건화 / avg-앵커 pseudo-labeling | EXP-001~003                   | **기각.** 게다가 avg 정의 자체가 틀렸었다                       |
| R5  | sim 홀드아웃으로 모델 선택                 | EXP-002, EXP-008, EXP-010A, **EXP-012** | **금지.** 네 번 역방향. 아키텍처·하이퍼 선택에 쓰지 말 것 |
| R6  | **사전학습** 인코더(effb0)가 전이를 개선 | EXP-011 + EXP-012 | **기각.** LB 7.3218 (armC 4.1018 대비 +3.22). 갭 6.693으로 처음부터 학습한 UNetSmall(6.275)보다도 **나쁘다** — 아래 참조 |
| C1  | H1 — CNN 레벨 분류기                | EXP-013 + EXP-014 | **채택·완료.** p 0.8983 → 0.9806(홀드아웃), LB 4.1018 → **3.6332** (−0.469). 유효 p는 0.9551로 2.55pp 할인됐다. 이 저장소 **최초로 선판정이 리더보드로 전이된 사례** |


### R6 상세 — sim 구조 ↔ real 갭의 단조 반전

세 백본 모두 AdaBN 적용 상태에서, 예산식 역산 `gap = √(LB² − s_sim² − (1−p)·63.77)`:


| 백본                       | sim 구조 | real 갭   | LB     |
| ------------------------ | ------ | -------- | ------ |
| PlainMLP (EXP-010 armC)  | 2.084 (최악) | **2.445** (최선) | 4.1018 |
| UNetSmall (EXP-010 armA) | 1.8875 | 6.275    | 7.0314 |
| effb0 사전학습 (EXP-012)     | 1.5186 (최선) | **6.693** (최악) | 7.3218 |


**3점이 정확히 역방향이다.** 우연이 아니라 단조 반전이므로, 모델이 배우는 것은 깊이 구조가
아니라 **sim 렌더러 고유의 텍스처 단서**로 읽어야 한다. 공간 귀납편향이 강한 백본일수록 그
단서를 잘 흡수하고, ImageNet 사전학습은 (텍스처 편향 때문에) 그 흡수를 오히려 **촉진**했다 —
H9가 걸었던 "실제 사진 특징이라 sim 텍스처에 덜 과적합" 가설의 정반대다.

effb0는 sim 우위로 갭 여유 0.386(손익분기 2.831)을 벌어둔 상태였는데 실제 6.693으로 **2.4배**
초과했다. 하이퍼파라미터나 백본 변형으로 좁힐 격차가 아니다. **남은 유효 레버는 갭 축과
레벨 축뿐이다.**

---

## 기입 규칙

- 한 항목에 **겨냥하는 예산 성분**과 **그 성분에서 계산한 기대 이득**을 반드시 적는다.
계산할 수 없으면 "미지"로 적고 왜 미지인지 쓴다 — 추측치를 숫자처럼 적지 않는다.
- **제출 없이 선판정할 수 있는지**를 명시한다. real→real 검증이 가능한 후보가 항상 우선이다.
- 실행되면 report_id를 적고 활성에서 기각·종료로 옮긴다. **기각 사유는 지우지 않는다** —
재논의를 막는 것이 이 절의 존재 이유다.
- 예산 표는 새 최고 기록이 나올 때마다 갱신한다(그래야 기대 이득이 유효하다).

